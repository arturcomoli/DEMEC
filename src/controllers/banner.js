var slider = tns({
  container: ".banner",
  items: 1,
  speed: 600,
  autoplay: true,
  autoplayTimeout: 3500,
  autoplayButtonOutput: false,
  swipeAngle: false,
  navPosition: "bottom",
  controls: false,
});

// console.log(slider.getInfo());

//   export {slider}
// module.exports = {slider}
